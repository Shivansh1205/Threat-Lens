"""AI / explainability package (Phase 6): LLM explanations + chatbot.

See ``ollama_client.py`` for the Ollama HTTP boundary, ``explainability.py``
for ``ExplainabilityEngine`` (per-alert explanations, run as a background
task), and ``chatbot.py`` for ``ChatbotModule`` (retrieval-augmented chat).
"""
